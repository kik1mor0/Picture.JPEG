ЦЕЛЬ ПРОЕКТА: Создание полиглот-файла, который одновременно является:

Визуально - картинкой (PNG/JPEG формата)

Функционально - исполняемым скриптом (BAT/CMD файлом)

При открытии - запускает командную строку Windows и выполняет команды

КОНКРЕТНАЯ ЗАДАЧА: При двойном клике на файл картинка.png открывается окно командной строки (cmd.exe) и выводится слово "привет".
